'use client';

// Firebase is not used in this project (Supabase is used instead).
// This component is a no-op stub kept for compatibility.
export function FirebaseErrorListener() {
  return null;
}
